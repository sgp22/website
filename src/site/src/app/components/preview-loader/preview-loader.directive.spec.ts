import { PreviewLoaderDirective } from './preview-loader.directive';

describe('PreviewLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
